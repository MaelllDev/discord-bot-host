{
  "name": "botpanel",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "description": "Painel web privado para hospedar e administrar bots e aplicações isoladas em Docker",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace web && npm run build --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "test:e2e": "npm run test:e2e --workspace server",
    "dev": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "start": "node server/dist/index.js"
  },
  "engines": {
    "node": ">=22.6"
  }
}
